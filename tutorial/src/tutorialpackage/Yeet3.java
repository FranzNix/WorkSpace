package tutorialpackage;

public class Yeet3 {

	public static void main(String[] args) {
		//Assume this could have any value between 0 and 24:
//		int time = 18;
//		String timeOfDay; 
//		if (time > 5 && time < 12) {
//			timeOfDay = "Morning";}
//		else if (time > 12 && time < 20) {
//			timeOfDay = "Daytime";}
//		else {
//			timeOfDay = "Night";}
//		System.out.println(timeOfDay);
		//TODO: set timeOfDay to the correct String value. 
//--------------------------------------------------------------------		
//		int weekday = 7; 
//		boolean holiday = false; 
//		int dayOfTheWeek;
//		if (holiday == false && weekday < 5) {
//		System.out.println("Wake up at 7:00");}
//		else {
//			System.out.println("Sleep in!");}
//--------------------------------------------------------------------
//		public boolean isPrime(int n) {}	// This creates a public boolean signature and it has a single integer which is n
//---------------------------------------------------------------------			

		public class Yeet4 {
			 public static double absoluteValue(double x) {	//This calls the public variable called absoluteValue.
				 if (x < 0) {	//This if statement tells if x is lesser than 0
					 return -x;}	//This will return x if its less than 0
				 else {	//This else statement will be printed out if the if statement isn't satisfied 
					 return x;}}	//This will return x is its greater than 0
			 public static void main(String[] args) {	//This will  help the codes in the parameters
					absoluteValue(9);	//This will call the absoluteValue which is 9
					System.out.println(absoluteValue(0));	//This will be print out the absoluteValue

		
	}

}
